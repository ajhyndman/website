// @flow

const defaultSize = 16;

export const breakpoint = {
  medium: '768px',
};

export const fontsize = {
  body: `${defaultSize}px`,
  h1: `${defaultSize * 2}px`,
  h2: `${defaultSize * (5 / 3)}px`,
  h3: `${defaultSize * (4 / 3)}px`,
}
