// @flow

const defaultSize = 14;

export const breakpoint = {
  medium: '768px'
};

export const fontSize = {
  body: defaultSize,
  h1: defaultSize * 2,
  h2: defaultSize * (5 / 3),
  h3: defaultSize * (4 / 3)
};
